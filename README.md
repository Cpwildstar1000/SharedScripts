# How to use

Run .ps1 as admin account. If you are getting an execution policy error then run the .cmd file from the same location as the .ps1 file


# DNS Check

If the DNS names do not match then test connections to computer and make sure that the ip is assigned to the computer then continue if it is
